import SimpleGoogleTranslator from "@/components/SimpleGoogleTranslator";

const LanguageToggle = (_props: { compact?: boolean }) => {
  return <SimpleGoogleTranslator />;
};

export default LanguageToggle;
